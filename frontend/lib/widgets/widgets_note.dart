// This file documents the purpose of the `lib/widgets` directory.
//
// Purpose:
// - Store small, reusable UI widgets that are globally available across
//   the app's screens (for example: loading indicators, global progress
//   bars, app-level snackbars, placeholders, common button styles, etc.).
// - Keep widgets here framework-agnostic and composable so they can be
//   used from any screen or feature module without duplication.
//
// Usage guidance:
// - Add widgets that represent UI primitives or app-wide components.
// - Export commonly used widgets from a single barrel file if desired.
// - Avoid placing feature-specific screens or heavy business logic here.

// Example: `LoadingIndicator`, `GlobalProgressBar`, `AppSnackbar`.

// This is a documentation-only file; delete or convert to README.md if
// you prefer a markdown note instead.
